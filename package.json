{
  "name": "erela.js-apple",
  "version": "1.0.0",
  "description": "This a plugin for Erela.JS to allow the use of AppleMusic URL's, it uses direct URL's being tracks, albums, and playlists and gets the YouTube equivalent.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "publish:stable": "yarn build && yarn publish --access=public"
  },
  "keywords": [
    "erela.js",
    "spotify",
    "plugin"
  ],
  "author": "Batman♥#0069",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.4",
    "cheerio": "*",
    "erela.js": "^2.3.3"
  },
  "devDependencies": {
    "@types/node": "^16.7.11",
    "typescript": "^4.4.2"
  }
}
